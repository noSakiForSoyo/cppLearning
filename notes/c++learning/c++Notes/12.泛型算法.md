### lambda
```cpp
[] (){
return ?;
}
```
[]作为捕获列表，内部传入外部的值作为指明变量
()作为传参列表，用于设定函数值

可以通过调用find_if来联合搜查
```cpp
find_if(a.begin,a.end(),[sz] (const int &a){
	return a.size() >= sz;
})
```
#### 隐式捕获
[=]值捕获
[&]引用捕获