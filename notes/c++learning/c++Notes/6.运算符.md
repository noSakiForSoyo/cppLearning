### 位运算符
```cpp
`expr //取反
expr1 << expr2 //左移
expr1 >> expr2 //右移
expr & expr //位与
expr ^ expr //位异或
expr | expr //位或  
```
推荐用作无符号类型的处理
### sizeof运算符
返回一个表达式或者类型所占的字节数，所得类型为size_t
```cpp
int data,*p;
//以下三个同义
sizeof(int);
sizeof data；
sizeof *p;

sizeof p;//指针所占的空间
```