### 三种智能指针
#### shared_ptr unique_ptr weak_ptr
允许多个指针指向同一个量
```cpp
shared_ptr<T> sp
if(sp){
	T a = *sp;
}
sp->mem;
sp.get();//返回sp中保存的指针
sp.swap(q);//·交换指针
```
创建
```cpp
auto p = std::make_unique<int>(42);
auto p = std::make_shared<int>(42);
```
