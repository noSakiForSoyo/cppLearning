#### 读取灰度图
```cpp
a = cv::imread("../a.jpg",IMREAD_GRAYSCALE);

namedWindow("image",WINDOW_AUTOSIZE);

imshow("image",a);

waitKey();
```
```cpp
//取·部分区域
Rect r(10,10,100,100);
Mat smallImg = a(r);
//rgb转灰度
cvtColor(a,c,COLOR_BGR2GRAY);
```